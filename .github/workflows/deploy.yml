name: Privacy Shield | Top VPNs Compared

on:
  push:
    branches:
      - main  # 只有推送到 main 分支时才触发

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies 🔧
        run: |
          pip install -r requirements.txt

      - name: Build Site 🏗️
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}  # 从 Secrets 读取 Key
        run: |
          python main.py

      - name: Deploy to GitHub Pages 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public  # 把生成出来的 public 文件夹推送到 Pages
          cname: compare.ii-x.com # 确保 CNAME 正确
